All .class files are compiled versions of .java sources file.

Class Discription:-

Sched :- contains Main class which take argument, file input and run code.
Scheduler :- contains Implementation of three algorithms (FCFS,SJF,RR).
SortBy* :- contains all sorting lambda functions.
Tast:- contain pid,arrival time and cpu burst.

HOW TO RUN:-

.class files are already generated. So, for run :-

java Sched Sched input.txt FCFS